import { TokenPage } from './token-page'

export const metadata = {
  title: 'RELAY Token',
  description: 'RELAY token contract address and information',
}

export default function Page() {
  return <TokenPage />
}
